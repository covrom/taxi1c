Управляемые формы (синтаксис)

Управляемые формы состоят из описания компонентов.
Тип компонента пишется без отступа, затем пишется его имя (правила как у наименования переменных).
Параметры компонента пишутся с одинарным отступом: имя параметра, пробел, далее его значение (как в Го).
Например:

Container Основной

